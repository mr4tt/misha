{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "typings": "index.d.ts",
  "scripts": {
    "clean": "rm -f ./*.d.ts",
    "watch": "npm run clean && tsc --watch",
    "build": "npm run clean && tsc --build",
    "copy-server-files": "rm -r ../mishamobile/src/server && mkdir -p ../mishamobile/src/server && cp *.js ../mishamobile/src/server && cp *.d.ts ../mishamobile/src/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.6.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
