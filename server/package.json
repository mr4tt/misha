{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -f ./*.d.ts",
    "watch": "npm run clean && tsc --watch",
    "build": "npm run clean && tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.6.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
